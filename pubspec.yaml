name: persistence_types
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^1.1.6
  sqflite: ^2.2.0+3
  cupertino_icons: ^1.0.2
  path: ^1.8.2
  floor: ^1.3.0
  firebase_core: ^2.3.0
  cloud_firestore: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  floor_generator: ^1.3.0
  build_runner: ^2.1.2

flutter:

  uses-material-design: true


  assets:
    - assets/

